name: Auto Rollback

on:
  workflow_dispatch:

jobs:
  rollback:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Rollback release
        run: node .github/scripts/rollback-release.js
